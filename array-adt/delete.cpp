#include <iostream>
using namespace std;
dlt (int arr[], int index){
  // int x;
  int length=6;
  for(int i=index;i<length-1;i++){
    arr[i]=A[i+1];
  }
  length++;
  
}
int main(){
   int arr[6]={2, 3, 4, 5, 6, 7} 
  //  for(int i=0;i<arr[])
   index=2;
cout<<dlt(arr, index);
return 0;
}