// i have to also check multipe elementleft might over after looping it..

class Single{
  public:
  void Merge(int A[], int asize, int C[], int csize){
    int i=j=k=0;
    while(i<asize && j<bsize){
      if()
    }
  }
};
#include <iostream>
using namespace std;
int main(){
  Single s;
  int a={21, 24, 29, 32, 40, 19, 27, 34, 45}
  int asize=10;
  int *C=new int[asize];
  s.Merge(A, asize, C, asize);
return 0;
}